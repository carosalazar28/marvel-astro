<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Plan de Cine MCU | Próximos estrenos</title>
		<meta name="description" content="Organiza tu ruta semanal de películas y series del MCU antes del próximo estreno." />
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&family=Space+Grotesk:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600;700&display=swap" rel="stylesheet">
	</head>
	<body>
		<slot />
	</body>
</html>

<style>
	:root {
		--primary-gradient-start: #11152a;
		--primary-gradient-mid: #1a1f3a;
		--primary-gradient-end: #11152a;
		--accent-color: #717cff;
		--accent-color-rgb: 113, 124, 255;
		--text-primary: #eef0f7;
		--text-secondary: #b8bfde;
		--text-muted: #96a0c5;
		--title-gradient-start: #eef0f7;
		--title-gradient-mid: #c7bdff;
		--title-gradient-end: #8991ff;
		--value-gradient-start: #ffffff;
		--value-gradient-end: #e0e7ff;
		--item-bg-start: rgba(17, 21, 42, 0.92);
		--item-bg-end: rgba(26, 31, 58, 0.92);
		
		/* Font Variables */
		--font-primary: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
		--font-display: 'Space Grotesk', 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
		--font-mono: 'JetBrains Mono', 'SF Mono', 'Monaco', 'Inconsolata', 'Roboto Mono', monospace;
	}

	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
		background: #0d1020;
		color: var(--text-primary);
		font-family: var(--font-primary);
		min-height: 100vh;
	}

	* {
		box-sizing: border-box;
	}

	body {
		padding: 0;
	}
</style>
